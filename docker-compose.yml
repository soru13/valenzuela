version: "3"

volumes:
  local_postgres_data: {}
  local_postgres_data_backups: {}

services:
  insert:
    build:
      context: .
      dockerfile: ./compose/insert/Dockerfile
    image: insert_area_local
    volumes:
      - .:/app
    networks:
      - transparencia_default
  insert_programas:
    build:
      context: .
      dockerfile: ./compose/insert/programas_sociales/Dockerfile
    image: insert_area_local_programas
    volumes:
      - .:/app
    networks:
      - transparencia_default
  transparencia:
    build:
      context: .
      dockerfile: ./compose/transparencia/Dockerfile
    image: transparencia_area_local
    volumes:
      - .:/app
networks:
  transparencia_default:
    external: true
